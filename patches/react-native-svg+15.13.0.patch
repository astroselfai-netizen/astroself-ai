diff --git a/node_modules/react-native-svg/android/.project b/node_modules/react-native-svg/android/.project
new file mode 100644
index 0000000..5e775b8
--- /dev/null
+++ b/node_modules/react-native-svg/android/.project
@@ -0,0 +1,28 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<projectDescription>
+	<name>react-native-svg</name>
+	<comment>Project react-native-svg created by Buildship.</comment>
+	<projects>
+	</projects>
+	<buildSpec>
+		<buildCommand>
+			<name>org.eclipse.buildship.core.gradleprojectbuilder</name>
+			<arguments>
+			</arguments>
+		</buildCommand>
+	</buildSpec>
+	<natures>
+		<nature>org.eclipse.buildship.core.gradleprojectnature</nature>
+	</natures>
+	<filteredResources>
+		<filter>
+			<id>1759293101986</id>
+			<name></name>
+			<type>30</type>
+			<matcher>
+				<id>org.eclipse.core.resources.regexFilterMatcher</id>
+				<arguments>node_modules|\.git|__CREATED_BY_JAVA_LANGUAGE_SERVER__</arguments>
+			</matcher>
+		</filter>
+	</filteredResources>
+</projectDescription>
diff --git a/node_modules/react-native-svg/android/build.gradle b/node_modules/react-native-svg/android/build.gradle
index 4fe912c..e9ad692 100644
--- a/node_modules/react-native-svg/android/build.gradle
+++ b/node_modules/react-native-svg/android/build.gradle
@@ -38,15 +38,29 @@ def safeExtGet(prop, fallback) {
 }
 
 def resolveReactNativeDirectory() {
+  // Try rootProject.ext first
   def reactNativeLocation = safeExtGet("REACT_NATIVE_NODE_MODULES_DIR", null)
   if (reactNativeLocation != null) {
     return file(reactNativeLocation)
   }
 
+  // Try to find react-native in the root node_modules first (relative to react-native-svg/android)
+  def rootReactNative = file("$rootDir/../../node_modules/react-native")
+  if (rootReactNative.exists() && rootReactNative.isDirectory()) {
+    return rootReactNative
+  }
+
+  // Try absolute path from project root
+  def projectRoot = rootProject.projectDir.parentFile
+  def absoluteReactNative = new File(projectRoot, "node_modules/react-native")
+  if (absoluteReactNative.exists() && absoluteReactNative.isDirectory()) {
+    return absoluteReactNative
+  }
+
   // Fallback to node resolver for custom directory structures like monorepos.
   def reactNativePackage = file(
       providers.exec {
-          workingDir(rootDir)
+          workingDir(projectRoot)
           commandLine("node", "--print", "require.resolve('react-native/package.json')")
       }.standardOutput.asText.get().trim()
   )
@@ -85,7 +99,7 @@ def getFrescoVersion() {
 def FRESCO_VERSION = getFrescoVersion()
 
 android {
-    compileSdkVersion safeExtGet('compileSdkVersion', 28)
+    compileSdk safeExtGet('compileSdkVersion', 28)
     namespace "com.horcrux.svg"
     def agpVersion = com.android.Version.ANDROID_GRADLE_PLUGIN_VERSION
     if (agpVersion.tokenize('.')[0].toInteger() >= 8) {
@@ -103,14 +117,14 @@ android {
     }
 
     defaultConfig {
-        minSdkVersion safeExtGet('minSdkVersion', 16)
+        minSdk safeExtGet('minSdkVersion', 16)
         //noinspection OldTargetApi
-        targetSdkVersion safeExtGet('targetSdkVersion', 28)
+        targetSdk safeExtGet('targetSdkVersion', 28)
         buildConfigField "boolean", "IS_NEW_ARCHITECTURE_ENABLED", isNewArchitectureEnabled().toString()
 
         consumerProguardFiles 'proguard-rules.pro'
     }
-    lintOptions {
+    lint {
         abortOnError false
     }
 
